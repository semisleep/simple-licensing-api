<%
if (typeof data === 'undefined') {
  var data = null;
}
if (typeof title === 'undefined') {
  var title = null;
}
if (typeof noHeader === 'undefined') {
  var noHeader = false;
}
if (typeof noFooter === 'undefined') {
  var noFooter = false;
}
%>

<!DOCTYPE html>
<html>
<head>
  <%- include('partial/html-head') %>
</head>

<body>

<div class="layout-body layout-<%= entry %>-body">

  <div id="app">
  </div>

</div>

<script type="text/javascript">

  window.preload = {
    user: <%- JSON.stringify(session.user || null) %>,
    data: <%- JSON.stringify(data) %>
  };

</script>

<%- include('partial/scripts') %>

<%- scriptTag(entry + '.js') %>

</body>
</html>